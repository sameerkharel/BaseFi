# BaseFi

BaseFi is a read-only Base portfolio dashboard that aggregates lending, LP, staking, and reward positions across major DeFi protocols.

## Planned structure

- `apps/web` - Next.js dashboard
- `packages/protocols` - protocol adapters
- `packages/shared` - shared types and utilities
