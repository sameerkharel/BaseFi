{
  "name": "basefi",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.0.5",
    "turbo": "^2.0.12",
    "typescript": "^5.5.4"
  }
}
